# SQL Queries
query.getAllUsers=SELECT id, name, email FROM users;
query.getUserById=SELECT id, name, email FROM users WHERE id = ?;
query.insertUser=INSERT INTO users (name, email) VALUES (?, ?);
query.updateUser=UPDATE users SET name = ?, email = ? WHERE id = ?;
query.deleteUser=DELETE FROM users WHERE id = ?;